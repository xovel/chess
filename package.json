{
  "name": "chess",
  "version": "0.0.0",
  "description": "Chess for fun.",
  "main": "main.js",
  "private": true,
  "scripts": {
    "update": "node update && node list",
    "dev": "npm run update && node app",
    "start": "npm run dev",
    "gen": "node gen --cdn",
    "generate": "node gen --cdn",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "chess"
  ],
  "author": "xovel",
  "license": "MIT",
  "devDependencies": {
    "cheerio": "^1.0.0-rc.2",
    "copy": "^0.3.1",
    "del": "^3.0.0",
    "express": "^4.16.3"
  },
  "dependencies": {
    "undici": "^5.10.0"
  }
}
